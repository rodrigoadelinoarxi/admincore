<odoo noupdate="1">
    <record id="ir_cron_fleet_alerts" model="ir.cron">
        <field name="name">Fleet: Generate fleet alerts</field>
        <field name="model_id" ref="model_fleet_vehicle"/>
        <field name="state">code</field>
        <field name="code">model.run_scheduler_fleet()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="numbercall">-1</field>
        <field name="doall" eval="False"/>
        <field name="nextcall" eval="(DateTime.now().replace(day=1, month=8, hour=0, minute=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
    </record>
</odoo>
